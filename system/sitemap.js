class Sitemap {
  constructor(site) {
    // TODO: If a sitemap.xml is found in application directory, use this sitemap.
    // Provide a final sitemap, that adds every sitemap for the site :)
    // Thus making sure that the sitemap does not exceed 50,000 links or 10mb uncompressed.
    // The final sitemap can include files that are larger, but for the final - make sure we do not exceed and making able to split :)

    
  }
}

module.exports = Sitemap
